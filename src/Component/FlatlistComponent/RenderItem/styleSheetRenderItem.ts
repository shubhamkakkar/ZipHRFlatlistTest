import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  card: {
    flex: 1,
    margin: 10,
    padding: 10,
  },
  cardDescription: {
    marginHorizontal: 20,
    marginTop: 20,
  },
});

export default styles;
